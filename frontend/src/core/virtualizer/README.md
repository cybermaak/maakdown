# Virtualizer

P5 implements the dynamic-height block virtualizer here.

Required behavior:

- bounded mounted block count
- virtualizer-aware anchor navigation
- multi-pass scroll stabilization
- visible range reporting for scroll-spy and progressive enhancement
