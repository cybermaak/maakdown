Pill status badge for document/sync state, mapped to the callout accent hues.

```jsx
<Badge tone="success" dot>Synced</Badge>
<Badge tone="warning">3 unresolved</Badge>
<Badge>Draft</Badge>
```

Tones: `neutral` / `info` / `success` / `warning` / `danger`. `dot` adds a leading status dot.
